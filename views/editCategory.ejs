<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Category</title>
    <style>
        body {
            background-color: #ffffff; /* Set background color to white */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        form {
            width: 50%;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 8px;
            background-color: #f9f9f9;
        }
        form div {
            margin-bottom: 10px;
        }
        label {
            display: block;
            font-weight: bold;
        }
        input[type="text"],
        input[type="number"],
        textarea {
            width: 100%;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            font-size: 16px;
        }
        button[type="submit"] {
            padding: 10px 20px;
            background-color: #007bff;
            color: #ffffff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
        }
        button[type="submit"]:hover {
            background-color: #0056b3;
        }
        h1
        {
          margin-bottom: 500px;
          text-align: center;
          position:absolute;
        }
    </style>
</head>
<body>
    <h1>Edit Category</h1>
    <!-- Form for editing a category -->
    <form action="/category/<%= category._id %>" method="POST"> <!-- Specify the correct route for updating a category -->
        <div>
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="<%= category.name %>"> <!-- Populate the input with the current category name -->
        </div>
        <div>
            <label for="description">Description:</label>
            <textarea id="description" name="description"><%= category.description %></textarea> <!-- Populate the textarea with the current category description -->
        </div>
        <div>
            <label for="slug">Slug:</label>
            <input type="text" id="slug" name="slug" value="<%= category.slug %>"> <!-- Populate the input with the current category slug -->
        </div>
        <div>
            <label for="order">Order:</label>
            <input type="number" id="order" name="order" value="<%= category.order %>"> <!-- Populate the input with the current category order -->
        </div>
        <button type="submit">Save Changes</button> <!-- Button to submit the form for updating the category -->
    </form>
</body>
</html>
